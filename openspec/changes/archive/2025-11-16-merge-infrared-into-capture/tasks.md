## 1. Move requirements
- [x] 1.1 Copy all requirements and scenarios from `infrared-capture` into `chissu-cli-capture`.
- [x] 1.2 Add a short stub/redirect in `infrared-capture` pointing to the merged capability.

## 2. Spec deltas
- [x] 2.1 Record added/modified requirements for `chissu-cli-capture`.
- [x] 2.2 Record removed/stubbed requirements for `infrared-capture`.

## 3. Validation
- [x] 3.1 Run `openspec validate merge-infrared-into-capture --strict`.
