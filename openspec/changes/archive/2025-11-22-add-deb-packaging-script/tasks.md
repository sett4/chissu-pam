1. [x] Scaffold `build/package-deb.sh` (or similar) that accepts `--distro (debian|ubuntu)` and `--version` flags, builds release binaries with `CARGO_HOME="$(pwd)/.cargo-home"`, stages files under `build/package/<distro>`, and runs `dpkg-buildpackage -us -uc` to emit `.deb` files into `dist/`.
2. [x] Add Debian packaging assets (`debian/control`, `rules`, `compat`, `changelog`, `postinst`, `postrm`, `prerm`, `install`) under `build/package/debian/` so maintainers can tweak metadata without editing the script; ensure dependencies include `libdlib-dev`, `libopenblas-dev`, `liblapack-dev`, `libgtk-3-dev`, `libudev-dev`, and `curl`.
3. [x] Implement a `postinst` step that skips bundling the dlib weights and instead downloads/uncompresses them at install time (unless already present) into `/var/lib/chissu-pam/dlib-models`, logging progress and exiting non-zero on failure.
4. [x] Document script usage plus the install-time download behaviour in `README.md` and, if needed, `docs/` so operators know packages remain small but fetch models on first install.
5. [x] Validation: run `openspec validate add-deb-packaging-script --strict`, `cargo fmt`, `cargo clippy -- -D warnings`, and `cargo test --workspace`; capture script dry-run output or resulting `.deb` listing for reviewers.
