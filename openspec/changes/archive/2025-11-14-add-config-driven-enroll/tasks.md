1. [x] Add a top-level `enroll` Clap command that defaults `--user` to the invoking account, rejects overrides unless running as UID 0, and accepts optional `--store-dir`, `--landmark-model`, `--encoder-model`, `--device`, and `--jitters` overrides.
2. [x] Implement a capture+extract helper that loads the PAM config (falling back to built-in defaults), captures a frame via the existing capture pipeline, runs descriptor extraction, and deletes temporary images after success.
3. [x] Wire the helper into the enrollment flow so descriptors are appended through the existing AES-GCM Secret Service lifecycle, reusing the same JSON output schema as `faces enroll` while adding capture metadata.
4. [x] Add unit/integration tests covering user resolution (root vs non-root), config/default usage, capture/extract error surfacing, and JSON payload contents.
5. [x] Document the new workflow in README/docs, and run `CARGO_HOME="$(pwd)/.cargo-home" cargo fmt`, `CARGO_HOME="$(pwd)/.cargo-home" cargo clippy -- -D warnings`, `CARGO_HOME="$(pwd)/.cargo-home" cargo test --workspace`, and focused suites (`cargo test -p chissu-cli`, `cargo test -p chissu-face-core`, `cargo test -p pam-chissu`).
