## 1. Rename capability
- [x] 1.1 Move `openspec/specs/face-features` to `openspec/specs/chissu-cli-faces` and update the spec title.
- [x] 1.2 Update any capability self-references to use `chissu-cli-faces`.

## 2. Spec delta
- [x] 2.1 Add a delta file under `changes/rename-face-capability/specs/chissu-cli-faces/spec.md` describing the rename.

## 3. Validation
- [x] 3.1 Run `openspec validate rename-face-capability --strict`.
